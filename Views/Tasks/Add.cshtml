@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Task — Web Planner</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@@tabler/icons-webfont@@latest/tabler-icons.min.css">
    <link rel="stylesheet" href="~/css/add-tasks.css">
</head>
<body>

<aside class="sidebar">
  <div class="brand">
    <div class="brand-icon"><i class="ti ti-calendar-event"></i></div>
    <span class="brand-name">Web Planner</span>
  </div>
  <div class="nav-section">
    <p class="nav-label">Menu</p>
    <a href="/Tasks/Index" class="nav-link"><i class="ti ti-checklist"></i> My Tasks</a>
    <a href="/Tasks/Add" class="nav-link active"><i class="ti ti-plus"></i> Add Task</a>
  </div>
  <div class="nav-section">
    <p class="nav-label">Filter</p>
    <a href="#" class="nav-link"><i class="ti ti-calendar-day"></i> Today</a>
    <a href="#" class="nav-link"><i class="ti ti-calendar-week"></i> This Week</a>
    <a href="#" class="nav-link"><i class="ti ti-flag"></i> High Priority</a>
  </div>
  <div class="sidebar-footer">
    <div class="user-row">
      <div class="avatar"></div>
      <div class="user-info">
        <div class="user-name"></div>
        <div class="user-email"></div>
      </div>
    </div>
    <a href="/Account/Login" class="nav-link" style="margin-top:4px;">
      <i class="ti ti-logout"></i> Sign out
    </a>
  </div>
</aside>

<main class="main">
  <div class="page-header">
    <a href="/Tasks/Index" class="back-btn"><i class="ti ti-arrow-left"></i> Back</a>
    <h1 class="page-title">Add Task</h1>
  </div>

  <div class="form-card">
    <div class="form-group">
      <label for="title">Title <span class="required">*</span></label>
      <div class="field">
        <i class="ti ti-pencil"></i>
        <input type="text" id="title" placeholder="What needs to be done?">
      </div>
    </div>

    <div class="form-group">
      <label for="description">Description</label>
      <textarea id="description" class="no-icon" placeholder="Add more details about this task…"></textarea>
    </div>

    <div class="form-row">
      <div class="form-group">
        <label for="priority">Priority</label>
        <div class="field">
          <i class="ti ti-flag"></i>
          <select id="priority">
            <option value="">No priority</option>
            <option value="low">Low</option>
            <option value="medium">Medium</option>
            <option value="high">High</option>
          </select>
        </div>
      </div>
      <div class="form-group">
        <label for="end-date">End date</label>
        <div class="field">
          <i class="ti ti-calendar"></i>
          <input type="date" id="end-date">
        </div>
      </div>
    </div>

    <div class="form-group">
      <label for="hashtags">Hashtags</label>
      <div class="field">
        <i class="ti ti-hash"></i>
        <input type="text" id="hashtags" placeholder="work, urgent, design (comma separated)">
      </div>
      <p class="hint">Separate tags with commas, no # needed.</p>
    </div>

    <div class="divider"></div>

    <div class="form-group">
      <label>Subtasks</label>
      <div class="subtask-list" id="subtask-list"></div>
      <div class="subtask-add">
        <div class="field" style="flex:1;">
          <i class="ti ti-plus"></i>
          <input type="text" id="subtask-input" placeholder="Add a subtask…">
        </div>
        <button class="add-btn" id="add-subtask-btn"><i class="ti ti-plus"></i></button>
      </div>
    </div>

    <div class="divider"></div>

    <div class="form-group">
      <label>Attach files</label>
      <div class="file-drop" id="file-drop">
        <i class="ti ti-cloud-upload"></i>
        <p>Drag and drop files here</p>
        <span>or click to browse</span>
      </div>
      <input type="file" id="file-input" multiple style="display:none;">
      <div id="file-list"></div>
    </div>

    <div class="form-actions">
      <button class="btn btn-primary" id="save-btn"><i class="ti ti-check"></i> Save task</button>
      <a href="/Tasks/Index" class="btn btn-ghost">Cancel</a>
    </div>
  </div>
</main>

<script src="~/js/add-task.js"></script>
</body>
</html>